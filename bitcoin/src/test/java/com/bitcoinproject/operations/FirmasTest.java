/**
 * @author Paola Merida
 * Fecha: 12/02/2026
 */
package com.bitcoinproject.operations;



import com.scriptbitcoin.interpreters.ScriptInterpreter;
import com.scriptbitcoin.interpreters.ExceptionsInterpreter;

class FirmasTest {
/* 
    @Test
    void opCheckSigShouldVerifySignature() {
        ScriptInterpreter interpreter = new ScriptInterpreter();
        // Test básico - puede necesitar datos de firma válidos
        assertThrows(ExceptionsInterpreter.class, () -> {
            interpreter.execute("OP_CHECKSIG");
        });
    }

    @Test
    void opCheckSigVerifyShouldVerifyAndConsume() {
        ScriptInterpreter interpreter = new ScriptInterpreter();
        assertThrows(ExceptionsInterpreter.class, () -> {
            interpreter.execute("OP_CHECKSIGVERIFY");
        });
    }

    @Test
    void opCheckMultiSigShouldVerifyMultipleSignatures() {
        ScriptInterpreter interpreter = new ScriptInterpreter();
        assertThrows(ExceptionsInterpreter.class, () -> {
            interpreter.execute("OP_CHECKMULTISIG");
        });
    } */
}